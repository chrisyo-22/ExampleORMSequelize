<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1> Student List</h1>
    <p>
        Total of Student: <%= total %>
            Current <%= page %> Page, Each Page <%= limit %> data
                    <ul>
                        <% datas.forEach(stu=> { %>
                            <li>
                                <%= stu.name %>
                            </li>
                            <% })%>
                    </ul>
    </p>
</body>

</html>